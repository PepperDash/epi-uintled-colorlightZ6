name: Branch Build

on:
  push:
    branches:
      - feature/*

jobs:
  build_project:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: Get-Location
      - run: docker run --rm pepperdash/sspbuilder echo $Env:GITHUB_WORKSPACE
