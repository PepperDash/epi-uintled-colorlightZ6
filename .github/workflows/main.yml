name: Branch Build

on:
  push:
    branches:
      - feature/*

jobs:
  build_project:
    runs-on: windows-latest
    container:
      image: pepperdash/sspbuilder
    steps:
      - uses: actions/checkout@v2
      - run: dir
      - run: c:\cihelpers\vsidebuild.exe -Solution "c:\github\workspace\eplugin-generic-colorlightZ6\pepperdash-generic-colorlightZ6-epi\pepperdash-generic-colorlightZ6-epi.sln" -BuildAll
